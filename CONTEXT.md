# DevGuide

面向开发者的技术栈学习 Agent。

为学习者提供技术栈资料和可核对依据的学习引导。本文记录已确认的业务术语；RAG 主流程与连续对话增强已实现，具体验证状态以需求和验收文档为准。

更新日期：2026-09-04

## Language

**技术栈清单**：已收录技术名称、分类和官方入口的目录。

**本地学习资料库**：已保存、可供学习问答检索复用的文档与项目资料集合。
_避免混称_：技术栈清单（目录收录不等于学习资料已入库）。

**本地优先问答**：每次学习提问先检索本地学习资料，资料不足时再联网补充的问答方式。

**追问**：延续已讨论的学习问题，要求进一步解释或补充资料的提问。

**资料充分性**：检索资料对当前学习问题关键点的覆盖程度。

**联网补充**：为当前问题获取本地资料不足部分的外部资料。

**资料回写**：将联网补充中经 Agent 筛选、有参考价值的来源原文片段保存到本地学习资料库，以供后续复用。

**引用依据**：支持回答内容的具体来源链接及对应原文片段。
_避免混称_：模型生成的摘要（摘要不等于来源原文）。

**技术适用范围**：资料或回答中的结论所适用的技术栈与使用语境。

**官方资料**：对应技术的作者、维护组织或标准组织发布的文档、标准说明及官方项目资料。

**第三方资料**：由对应技术官方主体之外的作者或组织发布的技术资料。

**公开展示访问**：无需登录即可浏览系统中已经收录、不会触发外部生成成本的项目内容。
_避免混称_：匿名生成（公开展示不允许触发模型、联网发现或资料回写）。

**生成访问**：用户完成认证后，能够触发动态发现、Agent 问答、联网搜索或资料回写的访问层级。

**站内筛选**：仅在已经加载的技术栈清单中按名称或分类缩小展示范围的本地操作。
_避免混称_：动态搜索（站内筛选不请求未收录内容，也不调用模型或外部搜索）。

**动态搜索**：针对尚未收录的技术名称发起实体核验、外部搜索并在通过后转正的生成访问操作。

**生成准入**：在调用模型、联网搜索或资料回写前，根据用户配额和实例并发上限决定是否允许执行的成本保护边界。
_避免混称_：完整抗 DDoS（生成准入保护下游额度，不替代边缘流量清洗、WAF、Bot Challenge 或登录安全）。

## Relationships

- 一个**技术栈清单**条目可以关联多份本地学习资料，也可能尚未关联资料。
- 一次**本地优先问答**先检索**本地学习资料库**，资料不足时使用**联网补充**。
- Agent 根据问题与检索片段判断**资料充分性**，存在明确缺口时针对缺口进行**联网补充**。
- 一份学习回答可以关联多条**引用依据**。
- **本地优先问答**允许跨技术栈检索，但必须向用户说明资料涉及的技术栈及**技术适用范围**，避免将框架特有用法泛化为语言通用规则。
- 通常在回答开头说明**技术适用范围**；存在会明显改变答案的关键歧义时，先向用户追问。
- **联网补充**中实际用于回答、成功取得原文且来源明确的资料，须经 Agent 筛选参考价值后才进行**资料回写**，并保留来源与技术适用范围。
- **资料回写**优先选择能补齐知识缺口、可独立理解并用于后续类似问题的原文片段；每次问答最多新增 3 个片段，无合适片段时不新增，重复内容直接复用。
- **资料回写**在回答完成后异步执行；新增片段可能稍晚才能检索到，回写失败不影响已经完成的回答。
- **联网补充**优先采用**官方资料**，不足时可以补充经 Agent 评估质量、来源明确且能获取原文的**第三方资料**，并向用户标明其第三方身份；两类资料回写时均遵循相同筛选规则。
- 联网后仍有证据缺口时，允许提供有引用支持的部分回答，并明确指出尚未确认的问题及缺少的资料。
- **追问**拥有独立的联网补充预算；用户明确要求更多内容时，应结合之前的问答重新联网补充，不能因上一轮预算已用完而停止扩展。
- **公开展示访问**只能读取已有技术栈和学习资料；任何会产生外部 API 成本或改变资料库的操作都属于**生成访问**。
- **公开展示访问**通过专用只读路径查询数据库已收录条目；详情可以读取有缓存的 GitHub Top10，但未知名称必须返回未找到，不能转入动态发现。
- **生成访问**当前使用 GitHub OAuth 认证；后续自有注册/登录方式属于独立身份体系增强，不改变公开展示与生成操作的边界。
- 匿名用户可以执行**站内筛选**；触发**动态搜索**或 Agent 问答时必须先登录，并由页面提供明确登录提示。
- **生成访问**在生产环境还必须通过**生成准入**；当前配额为每个 GitHub 用户每 UTC 日 10 次 Agent 请求、20 次动态搜索，全站最多同时执行 2 个生成任务，拒绝时不得调用下游服务。
- **生成准入**的规则判断与计数/并发状态存储必须保持可替换；M7 使用单实例内存状态，未来可换成共享存储、分布式限流和任务队列，而不修改 Agent 与动态搜索业务入口。

## Example dialogue

以下为已确认流程的示例：

> **开发者：** 用户提出学习问题时，从哪里开始查资料？
> **产品：** 先检索本地学习资料库；不足以回答问题时，再联网补充，并提供原文链接和原文内容。

> **开发者：** 面试官没有登录时可以看到什么？
> **产品：** 可以使用公开展示访问浏览已有内容；需要动态发现或 Agent 生成时，再完成登录进入生成访问。

## Flagged ambiguities

- “资料不足”：已明确为资料未能覆盖当前问题的关键点，由 Agent 判断；不能仅凭相关片段的命中数量或相似度认定资料充分。
- 本地检索范围：已允许跨技术栈检索；通常在回答开头标明涉及的技术栈和适用范围，有关键歧义时先追问。
- 联网资料复用：已明确按后续复用价值筛选，每次最多新增 3 个片段且不重复入库；该数量约束不限制本次回答参考多少资料。
- 资料来源：已确认官方优先、第三方补充，并明确标注第三方身份。
- “优先输出本地内容”：已明确本地资料优先用于回答，并附可核对的原文依据。
- “完整且全面”：以有依据地覆盖问题为目标；允许部分回答，必须说明尚未解决的关键点，不强行填补证据缺口。
- 追问上下文：当前页面临时保存会话，发送最近 3 轮已完成问答及引用；页面保留更早轮次并允许折叠，刷新或切换技术栈时清空。
- “公开访问”：已明确为**公开展示访问**，只读且不产生外部生成成本；不是匿名开放全部 API。
- “注册/登录”：当前生成访问使用 GitHub OAuth；后续必须增加的自有注册/登录方式、认证因子和账号恢复流程尚待专题设计。
- “搜索”：已拆分为匿名可用的**站内筛选**和必须登录的**动态搜索**，不能用同一权限规则笼统处理。
- “限流”：M7 已明确为保护模型与搜索额度的**生成准入**，不宣称其已经解决高并发扩展或 DDoS 防护。

详细需求见 [RAG 主流程增强](docs/requirements/PRD-rag-main-flow.md)。
