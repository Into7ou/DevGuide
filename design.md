# DevGuide 全站设计规范

- 更新日期：2026-09-12
- 状态：首页浅色四屏与目录混合布局均已确认并接入正式前端；本地构建与交互验证通过，未部署

## 规范地位

本规范替代第一轮石墨黑与冷蓝基线。新增与修改页面均遵守本文件；历史 design-system 文档仅作参考。设计以已确认的四屏独立原型为依据，不将原型中的未接入入口带入正式产品。

## 品牌、字体与视觉

- 名称统一为 DevGuide；柔白背景、墨黑文字、冷蓝强调，首页可出现浅蓝与淡紫光场过渡。
- 中文与英文优先采用同一字体家族：PingFang SC、Microsoft YaHei，之后回退系统无衬线字体。等宽字体只用于代码。
- 标题中等字重，行距约 1.2，避免中英文负字距过度压缩；桌面主标题约 56–68px，手机约 32–44px。
- 共享语义 token 管理背景、表面、文字、边框、按钮、间距和圆角。正文对比度至少 4.5:1，所有焦点可见。
- 不使用章节文案上方的编号或英文标签，不在顶部放暂停背景按钮。

## 首页结构与动画

1. 提出问题：左侧「从一个问题，到真正理解。」与简介、探索入口；右侧循环展示检索、充分性判断、整理引用及回答摘要。
2. 寻找依据：明确本地优先；资料充分直接回答，资料不足才联网补充。
3. 形成理解：固定回答示例与真实可点击引用。
4. 开始探索：技术栈入口和开发者联系信息。

首页首屏去除「过程演示」标签和「暂停演示」控件，保留循环动画；用低调固定流程说明避免误认为真实调用。系统减少动态效果时显示完整静态结果；离屏或页面不可见时停止循环。

使用原生滚动吸附，不拦截滚轮；背景按滚动进度连续改变颜色、光场位置与路径。手机、矮窗口或章节内容超过可用高度时取消吸附，允许完整阅读。动画不影响布局；路由离开时清理计时器、监听器及页面级滚动样式。

## 目录与内容页

全站采用柔白底、清晰文字层级、冷蓝强调与轻分隔线。目录使用混合布局：总览和方向概览承担探索，完整分组和搜索结果承担查找。不得把全部技术以同等大小卡片一次性铺满；详情与学习对话按各自用途布局，不机械套用目录。

### 信息层级

- 目录默认进入总览，侧栏保留总览入口。一级方向使用中文名称：前端开发、后端开发、人工智能、数据处理、部署运维；数据处理包含大数据相关用途。
- 方向下面按用途分组，名称与说明简短。概览每组最多预览 4 条，完整分组使用紧凑列表；正式首版每页 12 条。数量来自实际返回数据，不使用原型样例填充。
- 条目展示名称与简短用途，简介最多显示两行；完整内容进入详情阅读。开发者指定顺序优先，其他按名称排序，不用 LLM 输出的“热门”或“推荐”替代排序依据。
- 同一技术可从多个方向或用途分组进入，始终共用同一份详情。详情使用非交互的“技术标签”表达归属，不再展示“从这些方向找到它”的重复导航文案。
- 页头、搜索、分组与页脚使用统一对齐线；长名称、URL 与中文自然换行，代码区域局部横向滚动，整页不能横向溢出。

### 顶部栏与侧栏

- 全局顶部栏承载品牌、站点入口和账号。目录侧栏承载学习方向和用途分组，不重复全局功能。
- 侧栏在顶部栏实际高度下方展开；顶部高度用共享变量 --app-header-height 管理，随窗口与内容变化更新，禁止在页面里重复写死偏移。
- 桌面保留窄导航栏，图标配短中文名称。展开面板在窄栏旁悬浮覆盖，按钮位置和正文宽度不变，避免悬停时点击目标移动。
- 鼠标悬停临时展开，移出延迟约 220ms 收起；点击展开后保持打开。选择一级方向后立即显示其二级分组并保持展开，选择二级分组后收起。
- 移动端通过按钮展开抽屉；打开期间阻止背景滚动和交互，关闭或离开页面时完整恢复。两端都支持外部空白区域和 Escape 关闭。
- 键盘可操作全部导航；打开时焦点进入面板，切换方向时跟随所选项，关闭后回到可见按钮。移动抽屉打开时焦点保留在抽屉内部。
- 顶部栏高于目录侧栏，侧栏高于其外部遮罩，搜索浮层低于侧栏；所有层级集中维护，避免局部 z-index 互相竞争。

### 搜索、结果与状态

- 只保留一个搜索框，默认覆盖全部已收录技术，包含尚未确认分类的技术。限定当前方向或分组必须由用户明确选择。
- 范围使用持续可见的按钮和自定义浅色浮层，不使用风格割裂的系统下拉弹窗。显示具体方向/分组名称与选中状态，支持方向键、Enter、Escape、外部点击关闭。
- 切换范围保留关键词。局部无匹配时先提供“搜索全部技术”，只有全目录无匹配才提供明确的联网查询按钮；不因输入或按回车自动调用生成服务。
- URL 保存方向、分组、关键词、范围与页码。详情返回优先使用浏览历史恢复位置，直接访问详情时提供安全的目录回链。
- 保留加载、失败重试、空分组、空结果、登录提示。真实生成访问仍遵循服务端认证与生成准入，前端界面不能替代权限保护。

### 分类展示边界

分类映射与视觉组件分离。首版由 frontend/src/data/catalog-taxonomy.json 维护精确名称到用途分组的固定映射，只有实际 API 返回的条目才显示；旧 LLM category 不直接作为正式分组依据。未知条目保持可搜索，详情标明分类待确认。开发者使用 scripts/catalog-review.mjs 查看与调整映射，变更保留历史；生产构建需在配置更新后重新发布。

这是首版页面层的分类确认方式，并未新增服务端审核队列或自动证据审核。当前公开接口仍一次返回完整目录，前端分页控制呈现数量；更大规模时应独立增加服务端搜索分页，不宣称前端分页已经解决数据传输规模问题。

## 代码组织

- 首页负责章节编排；背景滚动、Agent 循环、引用示例按独立职责拆分。目录页面负责组合，侧栏、范围选择、技术条目独立复用；数据请求、筛选分页与分类映射各自维护。
- 复用已有 Vue 组件与 token，公共容器、按钮和卡片集中维护；页面 CSS 只负责局部布局。
- 不引入仅用于本效果的动画框架，不添加通用化但只有一次使用的抽象；正常格式化，避免压缩堆叠规则。
- 保留 API、OAuth、匿名展示、生成准入与对话行为。新增页面必须有有效入口，不保留原型假按钮。

## 页脚与验收

页脚与正文对齐，显示「如果有开发建议，请联系开发者。」及 mailto:1429246179@qq.com。

验收覆盖构建、现有业务回归、首页导航无生成调用、动画清理与减少动态效果、桌面与窄屏的真实浏览器布局。单纯 HTTP 200 或 DOM 已挂载不代表视觉验收通过。未执行的真实登录、AI 生成与人工视觉验收必须单独记录。

## 第二轮验证记录（2026-09-11）

- 构建无警告；11 个测试文件、42 项测试通过。
- Chrome 实际页面验证：1440px 与 390px 首页无横向溢出；背景由柔白随滚动过渡至淡紫；减少动态效果后取消吸附并显示完整结果；首页进入目录后场景与吸附样式清理。
- 2026-09-10 联调时目录读取 26 条真实数据，目录和 React 详情在桌面与窄屏无横向溢出；2026-09-11 后端未运行，未重复业务联调。
- GPT-5.6 Sol / medium 对抗审查已完成，返回目录丢失筛选问题已修复并有回归测试。
- 真实 OAuth 登录、模型生成及人工最终视觉验收未在本轮执行。不包含部署。

## 章节与路由过渡（2026-09-11）

首页章节链接统一通过 Vue Router 发起原生平滑滚动，避免原生锚点与路由历史恢复竞争；重复点击与往返必须保持连续背景插值，并扣除顶部导航高度。其他页面不保留首页滚动样式。

页面路径改变时使用约 320ms 淡出、480ms 淡入；进入与离开的页面各自保留布局，离开页不再响应操作，不延迟新页面挂载。不使用影响固定背景定位的 transform。查询参数变化不重挂载页面或重置滚动位置；后退恢复保存位置。

背景使用知识路径主题：桌面 12 个小粒子沿三条曲线缓慢流动，手机减至 6 个；路径与光场按不同幅度随滚动移动，形成轻微视差。使用 SVG 与 CSS，不引入动画框架。页面不可见时暂停粒子，离开首页移除。减少动态效果时取消平滑滚动、淡化、粒子与视差。

本次修复复测：原生锚点第 3 次导航曾被历史恢复改为 instant；修复后连续五次 2→1→2→1→2 均记录到平滑滚动和连续背景颜色。真实 Chrome 验证了淡入淡出的中间帧、粒子移动、1440px / 390px 无横向溢出与减少动态效果。

收尾记录（2026-09-12）：构建通过，11 个测试文件共 43 项测试通过，包含重复章节导航回归。对抗审查提出的同 hash 导航疑虑已通过真实浏览器与当前路由源码排除：手动滚离后点击相同章节仍平滑返回。


## 目录正式接入验证（2026-09-12）

- 正式 Vue 构建通过；54 项测试通过，覆盖分类映射、局部搜索边界、待分类搜索、详情返回和侧栏焦点/生命周期。
- Chrome 验证正式构建在1440px/390px下无横向溢出；总览、侧栏一级到二级导航、范围搜索回退、详情标签及返回筛选正常。已检查桌面与手机截图。
- 当前后端未运行，浏览器使用符合现有 DTO 的拦截测试数据；未重复真实数据库联调、OAuth 或模型调用。GPT-5.6 Sol / medium 审查发现的侧栏焦点问题已修复。
