# M5 用户体系 — 问题记录

> 阶段：M5 用户体系
> 更新日期：2026-09-04
> 状态：登录流程已完成本地联调；生产 Cookie、强加密、密钥轮换和环境模板已在 M7 前置处理中完成；首次部署限定单实例
> 本地端口更新（2026-09-03）：后端默认端口已调整为 18080，当前回调地址为 `http://localhost:18080/login/oauth2/code/github`；下文 8080 记录属于当时的联调环境。

---

## 当前进度

| 项 | 状态 |
|---|---|
| 后端骨架（Security + OAuth2 + users/user_tokens + Token 加密） | ✅ 编译通过，12 个测试全绿 |
| GithubClient 改用当前用户 Token | ✅ |
| 前端登录入口（AppHeader） | ✅ |
| 登录联调 | ✅ 已完成 GitHub 授权、用户写入、Token 加密保存与登录态展示 |
| API/MCP 鉴权与 CSRF | ✅ 2026-09-04 完成并通过回归测试 |
| 生产 Cookie、密钥与环境模板 | ✅ 2026-09-04 完成 |
| 首次部署会话方案 | ✅ 已决策为单实例；扩容前再引入共享会话 |

## 已解决的问题

### 1. 后端启动失败：`Hex-encoded string must have an even number of characters`

- **现象**：接入 OAuth2 + `TokenCipher` 后，后端启动时 `securityConfig` bean 创建失败，`Caused by: IllegalArgumentException: Hex-encoded string must have an even number of characters`。
- **根因**：`Encryptors.standard(key, salt)` 内部的 `DerivedSecretKeyGenerator` 会调用 `Hex.decode(salt)`，要求 salt 必须是**偶数长度的十六进制字符串**；原代码传的是普通字符串 `"techstack-agent"`（15 个字符，奇数），导致解码失败。
- **修复**：把固定盐值先转成 hex 再传入 —— `new String(Hex.encode("techstack-agent".getBytes(StandardCharsets.UTF_8)))`。密码 `key` 本身走 `PBEKeySpec`（PBKDF2），是作为普通口令使用的，不受 hex 限制，无需改动。
- **经验**：Spring Security 的 `Encryptors.standard/stronger` 的第二个参数是「hex 编码的盐」，不是任意字符串；填普通字符串会直接抛异常。

## 联调前准备（已完成）

1. 创建 GitHub OAuth App（callback URL 填 `http://localhost:8080/login/oauth2/code/github`）。✅
2. Client ID / Secret 已填入 `.env` 的 `GITHUB_OAUTH_CLIENT_ID` / `GITHUB_OAUTH_CLIENT_SECRET`。✅
3. 后端已重启（8080）、前端已重启（5173）。✅

## 联调验证步骤

1. 浏览器访问 `http://localhost:5173`，点右上角「登录」。
2. 跳转 GitHub 授权页（scope `read:user`），点 Authorize。
3. 回调 `/login/oauth2/code/github` → 后端 upsert `users` + 加密写入 `user_tokens` → 重定向回 `http://localhost:5173`。
4. 刷新后 `AppHeader` 调用 `/api/auth/me`，应显示头像 + 用户名。

## 头像不显示问题（已定位）

- **现象**：登录后用户名正常，但右上角头像破图，控制台报 `Failed to load resource: net::ERR_CONNECTION_CLOSED`。
- **结论**：**网络环境问题，非代码 Bug**。后端返回的 `avatarUrl` 是 `https://avatars.githubusercontent.com/...`，该域名（GitHub 图片 CDN）在当前网络被屏蔽，直接访问 GitHub 官网图片也不显示。
- **处理**：前端加优雅降级 —— `AppHeader` 头像加载失败（`@error`）时显示用户名首字母圆形占位，避免破图。

## 对抗性代码审查（已完成）

独立对抗性子代理对 M5 全部相关文件做了静态审查，共报 3 高危 + 8 中危 + 8 低危。逐条甄别后修复如下：

### 已修复（11 项）
| 编号 | 问题 | 修复 |
|---|---|---|
| H1 | 绑定 token 过期/撤销后无降级，401 永久报错 | `GithubTokenProvider` 加过期判断清理 + `invalidateCurrentUserToken()`；`GithubClient` 401 时清 token 并回退应用级 token 重试一次 |
| H2 | `dev-insecure-key` 默认密钥，漏配可被公开常量解密 | `TokenCipher` fail-fast，拒绝短密钥与占位值；新密文使用 `v2:` + `Encryptors.stronger`，并保留旧密文/历史密钥读取能力 |
| H3 | 登录链路硬编码 localhost | 后端 `app.frontend-base-url` 可配置；前端登录改相对路径 `/oauth2/authorization/github`；`vite.config.js` 加 `/oauth2` 代理 |
| M1 | CSRF 全站关闭 + API 全放行 | 2026-09-04 完成二次加固：API/MCP 默认要求登录，匿名业务请求返回 JSON 401；恢复 API CSRF 校验，前端写请求先获取并携带 CSRF Token |
| M2 | `updated_at` 永不更新 | 迁移到单行 upsert 后 `ON CONFLICT DO UPDATE ... updated_at = now()` 显式维护 |
| M3 | 并发首次登录唯一键冲突 500 | `UserMapper.upsertByGithubId` 用 `ON CONFLICT` 原子合并 |
| M4 | `/api/auth/me` 用 `Map.of` 遇 null 属性 NPE | 改 `HashMap` |
| M6 | token 落库异常中断登录 | `OAuth2SuccessHandler` 全程 try/catch + log，落库失败仍重定向 |
| M7 | user_tokens 无唯一约束、每次登录插一行 | `V4__token_lifecycle.sql`：去重 + `expires_at` 列 + `user_id` 唯一索引；`upsertByUserId` |
| M8 | 静默失败无日志 | 补 `log.warn`（缺 id / 缺用户 / 缺 authorized client） |
| G4 | 非整型 id 抛异常 | （低危，保留现状：GitHub id 恒为整型，`toLong` 对 Integer/Long 均安全） |

### 甄别为「报告有误」的 1 项
- **M5**：审查建议把 `authentication` 强转为 `OAuth2LoginAuthenticationToken` 直接取 client。**这是错的**——success handler 收到的是 `OAuth2AuthenticationToken`，强转会 `ClassCastException`。现有 `loadAuthorizedClient` 写法在单实例下是标准且正确的。

### M7 前置事项状态（原 M5 遗留）

| 编号 | 事项 | 当前状态 |
|---|---|---|
| M5 | 多实例会话/AHOT 共享 | **决策关闭**：首次 M7 部署限定单实例；横向扩容前必须迁移共享 Session 和 OAuth 授权信息 |
| M9 | 登出 / 解绑入口（前端按钮 + 后端 logout 清理 token） | **仍待处理**：不属于本次五项上线必修项 |
| M10 | 环境变量加载与生产模板 | **已完成**：本地启动脚本加载 `.env`，新增受保护的生产环境变量模板与说明 |
| G5 | 生产 Session Cookie | **已完成**：`prod` profile 强制 Secure、HttpOnly 与 SameSite=Lax |
| G7 | Token 强加密与密钥轮换 | **已完成**：版本化强加密、旧密文兼容读取和历史密钥轮换窗口均已实现 |

当前仍未关闭的 M5 事项只有 M9，以及未来横向扩容时才触发的共享会话改造；二者均不阻塞首次单实例 M7 部署。

---

> 注：联调过程中若暴露问题（如回调地址、会话、跨域等），将在联调后补充到本文件。
